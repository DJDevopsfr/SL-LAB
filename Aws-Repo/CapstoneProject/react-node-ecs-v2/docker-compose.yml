services:
  backend:
    build: ./backend
    ports: ["3001:3001"]
  frontend:
    build: ./frontend
    environment:
      BACKEND_URL: http://backend:3001
    ports: ["3000:80"]
    depends_on: [backend]
