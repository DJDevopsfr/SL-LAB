server {
 listen 80;
 root /usr/share/nginx/html;
 index index.html;
 location /api/ { proxy_pass ${BACKEND_URL}; proxy_set_header Host $host; }
 location / { try_files $uri /index.html; }
 location = /health { access_log off; return 200 "ok"; }
}
